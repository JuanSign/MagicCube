#include "src/cube/cube.hpp"
#include "src/algorithms/hill-climbing/hcsa.hpp"

int main()
{
    // CUBE
    // CUBE c; // instantiate a true-random cube
    // CUBE c(i); // for an integer i != 0, instantiate a pseudo-random cube with seed 0

    // HCSA
    /*
        c is a CUBE
        i is the choosen objective function (default is 0)
        <variable_name> is the prefered name of the HCSA instance
    */
    // HCSA <variable_name>(c,i);
    // <variable_name>.SET_OPTION(i); // set the objective function to option i
    // <variable_name>.CALCULATE(c); // calculate the objective function of CUBE c
}